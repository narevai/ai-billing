[**@ai-billing/polar**](../index)

***

[@ai-billing/polar](../index) / createPolarDestination

# Function: createPolarDestination()

> **createPolarDestination**\<`TTags`\>(`options`): `Destination`\<`TTags`\>

Defined in: [polar-destination.ts:71](https://github.com/narevai/ai-billing/blob/main/packages/polar/src/destination/polar-destination.ts#L71)

Creates a Destination that ingests billing events into Polar.

Identity is extracted from tags (internal `customerId`, plus optional `externalId`). If neither identity
is present, the event is skipped to avoid ingesting anonymous data.

## Type Parameters

### TTags

`TTags` *extends* `JSONObject` = `JSONObject`

The shape of the tags object, extending DefaultTags.

## Parameters

### options

[`PolarDestinationOptions`](../interfaces/PolarDestinationOptions)\<`TTags`\>

Destination configuration; see [PolarDestinationOptions](../interfaces/PolarDestinationOptions).

## Returns

`Destination`\<`TTags`\>

A destination function that sends events to Polar.
