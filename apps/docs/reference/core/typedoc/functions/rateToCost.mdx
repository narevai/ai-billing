[**@ai-billing/core**](../index)

***

[@ai-billing/core](../index) / rateToCost

# Function: rateToCost()

> **rateToCost**(`amount?`): [`Cost`](../interfaces/Cost)

Defined in: [packages/core/src/cost/convert-cost.ts:55](https://github.com/narevai/ai-billing/blob/main/packages/core/src/cost/convert-cost.ts#L55)

Creates a base-unit USD cost from a numeric amount.

## Parameters

### amount?

`number` = `0`

Amount in USD base units. Defaults to 0 when omitted.

## Returns

[`Cost`](../interfaces/Cost)

A `Cost` object with `unit: 'base'` and `currency: 'USD'`.
